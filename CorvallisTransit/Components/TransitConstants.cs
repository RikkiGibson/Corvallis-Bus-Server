using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CorvallisTransit.Components
{
    /// <summary>
    /// The constants we'll use in this application.
    /// Right now a few are littered around, lets try to drop them in here.
    /// 
    /// </summary>
    public static class TransitConstants
    {

        // thread time out we'll leave at 20 secs for now.
        // (this is in milliseconds)
        public const int ThreadTimeout = 20000;
    }
}
